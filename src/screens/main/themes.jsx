import { lightBlue, orange, red } from "@mui/material/colors";

export const lightTheme = {
  palette: {
    mode: "light",
    primary: red,
  },
};

export const darkTheme = {
  palette: {
    mode: "dark",
    primary: orange,
  },
};
